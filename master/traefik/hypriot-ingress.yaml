apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: hypriot
spec:
  rules:
  - host: hypriot
    http:
      paths:
      - path: /
        backend:
          serviceName: hypriot
          servicePort: 80